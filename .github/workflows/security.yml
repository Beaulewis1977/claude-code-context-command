name: Security Analysis

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  schedule:
    # Run security scans daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

env:
  NODE_ENV: test

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  # Static Application Security Testing (SAST)
  codeql-analysis:
    name: CodeQL SAST Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-extended,security-and-quality
          config-file: ./.github/codeql-config.yml

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
          upload: true

  # Comprehensive security test suite
  security-test-suite:
    name: Security Test Suite
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'scripts/package.json'

      - name: Install dependencies
        working-directory: ./scripts
        run: |
          echo "📦 Installing dependencies for security testing..."
          npm ci --only=dev

      - name: Run comprehensive security test suite
        working-directory: ./scripts
        run: |
          echo "🔍 Running comprehensive security test suite..."
          
          # Run the security tests with detailed output
          npm test 2>&1 | tee security-detailed-results.txt
          
          # Check exit status
          if [ ${PIPESTATUS[0]} -eq 0 ]; then
            echo "✅ All security tests passed"
            
            # Count passed tests
            PASSED_TESTS=$(grep -c "✓" security-detailed-results.txt || echo "0")
            echo "📊 Security tests passed: $PASSED_TESTS"
            
            # Check for specific security test categories
            echo "🛡️ Security test categories covered:"
            grep -E "describe|it.*should" security-detailed-results.txt | head -10
            
          else
            echo "❌ Security tests failed"
            echo "📋 Failure details:"
            grep -E "Error|Failed|×" security-detailed-results.txt || echo "No specific errors found"
            exit 1
          fi

      - name: Validate security module integrity
        working-directory: ./
        run: |
          echo "🔐 Validating security module integrity..."
          
          # Test security module functions
          node -e "
            import('./lib/security.js').then(security => {
              const tests = [
                () => security.InputValidator.validateMode('compact'),
                () => security.InputValidator.validateMode('standard'),
                () => security.InputValidator.validateMode('detailed'),
              ];
              
              tests.forEach((test, i) => {
                try {
                  test();
                  console.log(\`✅ Security test \${i + 1} passed\`);
                } catch (error) {
                  console.error(\`❌ Security test \${i + 1} failed:\`, error.message);
                  process.exit(1);
                }
              });
              
              console.log('✅ Security module integrity verified');
            }).catch(error => {
              console.error('❌ Failed to import security module:', error);
              process.exit(1);
            });
          "

      - name: Upload security test results
        if: always()
        uses: actions/upload-artifact@v4
        working-directory: ./scripts
        with:
          name: security-test-results-detailed-${{ github.run_id }}
          path: security-detailed-results.txt
          retention-days: 90

  # Dependency vulnerability scanning
  dependency-scan:
    name: Dependency Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'scripts/package.json'

      - name: Install dependencies
        working-directory: ./scripts
        run: npm ci

      - name: Run npm audit
        working-directory: ./scripts
        run: |
          echo "🔍 Running npm audit for vulnerability scanning..."
          
          # Run audit and capture results
          npm audit --audit-level=low --json > audit-results.json || AUDIT_EXIT=$?
          
          # Parse and display results
          if [ -f audit-results.json ]; then
            # Count vulnerabilities by severity
            HIGH=$(jq '.vulnerabilities | to_entries[] | select(.value.severity == "high") | .key' audit-results.json 2>/dev/null | wc -l || echo "0")
            MODERATE=$(jq '.vulnerabilities | to_entries[] | select(.value.severity == "moderate") | .key' audit-results.json 2>/dev/null | wc -l || echo "0")
            LOW=$(jq '.vulnerabilities | to_entries[] | select(.value.severity == "low") | .key' audit-results.json 2>/dev/null | wc -l || echo "0")
            
            echo "📊 Vulnerability Summary:"
            echo "  High severity: $HIGH"
            echo "  Moderate severity: $MODERATE"  
            echo "  Low severity: $LOW"
            
            # Fail on high or moderate vulnerabilities
            if [ "$HIGH" -gt 0 ] || [ "$MODERATE" -gt 0 ]; then
              echo "❌ High or moderate severity vulnerabilities found"
              echo "🔍 Vulnerability details:"
              cat audit-results.json | jq '.vulnerabilities' 2>/dev/null || echo "Error parsing vulnerability details"
              exit 1
            else
              echo "✅ No high or moderate vulnerabilities found"
            fi
          else
            echo "❌ Failed to generate audit results"
            exit 1
          fi

      - name: Check for known security issues
        working-directory: ./scripts
        run: |
          echo "🔍 Checking for known security patterns..."
          
          # Check package.json for dev dependencies in production
          if grep -E '"(mocha|test|dev)"' package.json | grep -v devDependencies; then
            echo "⚠️  Warning: Development dependencies found in production dependencies"
          fi
          
          # Check for any references to unsafe functions
          if grep -r "eval\|Function\|setTimeout.*string\|setInterval.*string" ../lib/ ../scripts/ 2>/dev/null; then
            echo "❌ Potentially unsafe function usage found"
            exit 1
          else
            echo "✅ No unsafe function usage detected"
          fi

      - name: Upload audit results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-audit-results-${{ github.run_id }}
          path: scripts/audit-results.json
          retention-days: 30

  # Secret scanning
  secret-scan:
    name: Secret Detection
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run secret detection
        run: |
          echo "🔍 Scanning for potential secrets and sensitive data..."
          
          # Common secret patterns
          SECRET_PATTERNS=(
            "password\s*[=:]\s*['\"][^'\"]+['\"]"
            "api[_-]?key\s*[=:]\s*['\"][^'\"]+['\"]"
            "secret\s*[=:]\s*['\"][^'\"]+['\"]"
            "token\s*[=:]\s*['\"][^'\"]+['\"]"
            "['\"][A-Za-z0-9]{32,}['\"]"
            "-----BEGIN.*PRIVATE KEY-----"
          )
          
          SECRETS_FOUND=0
          
          for pattern in "${SECRET_PATTERNS[@]}"; do
            echo "Checking pattern: $pattern"
            if grep -r -E -i "$pattern" . --exclude-dir=node_modules --exclude="*.md" 2>/dev/null; then
              echo "⚠️  Potential secret found with pattern: $pattern"
              SECRETS_FOUND=$((SECRETS_FOUND + 1))
            fi
          done
          
          if [ $SECRETS_FOUND -gt 0 ]; then
            echo "❌ $SECRETS_FOUND potential secrets found"
            echo "Please review and remove any hardcoded secrets"
            exit 1
          else
            echo "✅ No secrets detected"
          fi

      - name: Check for test secrets
        run: |
          echo "🔍 Checking for test secrets that might be real..."
          
          # Look for realistic-looking test data that might be real secrets
          if grep -r -E "(sk-|pk_|ghp_|gho_|ghu_|ghs_)" . --exclude-dir=node_modules 2>/dev/null; then
            echo "⚠️  Found patterns that look like real API keys/tokens"
            echo "Please verify these are actually test data and not real secrets"
          else
            echo "✅ No suspicious API key patterns found"
          fi

  # File permission and configuration security
  config-security:
    name: Configuration Security
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check file permissions
        run: |
          echo "🔒 Checking file permissions and security configuration..."
          
          # Check for overly permissive files
          find . -type f -perm /o+w 2>/dev/null | while read file; do
            echo "⚠️  World-writable file found: $file"
          done
          
          # Check installer script permissions
          INSTALLERS=(
            "installers/install.sh"
            "installers/install.ps1"
          )
          
          for installer in "${INSTALLERS[@]}"; do
            if [ -f "$installer" ]; then
              if [ ! -x "$installer" ] && [[ "$installer" == *.sh ]]; then
                echo "❌ Installer script not executable: $installer"
                exit 1
              else
                echo "✅ Installer permissions correct: $installer"
              fi
            fi
          done

      - name: Validate configuration files
        run: |
          echo "📋 Validating configuration file security..."
          
          # Check package.json for security issues
          if [ -f "scripts/package.json" ]; then
            echo "Checking package.json..."
            
            # Look for postinstall scripts (potential security risk)
            if grep -q '"postinstall"' scripts/package.json; then
              echo "⚠️  postinstall script found - review for security"
              grep -A2 -B2 '"postinstall"' scripts/package.json
            else
              echo "✅ No postinstall scripts found"
            fi
            
            # Check for scripts that might be dangerous
            if grep -E '"(rm|curl|wget|eval)"' scripts/package.json; then
              echo "⚠️  Potentially dangerous commands in package.json scripts"
            else
              echo "✅ Package.json scripts appear safe"
            fi
          fi

  # Security reporting
  security-report:
    name: Security Report
    runs-on: ubuntu-latest
    needs: [codeql-analysis, security-test-suite, dependency-scan, secret-scan, config-security]
    if: always()
    timeout-minutes: 5
    
    steps:
      - name: Generate security report
        run: |
          echo "📊 Security Analysis Report"
          echo "=========================="
          echo "Run ID: ${{ github.run_id }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo ""
          
          # Check job results
          echo "📋 Security Check Results:"
          echo "- CodeQL Analysis: ${{ needs.codeql-analysis.result }}"
          echo "- Security Test Suite: ${{ needs.security-test-suite.result }}"
          echo "- Dependency Scan: ${{ needs.dependency-scan.result }}"
          echo "- Secret Scan: ${{ needs.secret-scan.result }}"
          echo "- Config Security: ${{ needs.config-security.result }}"
          echo ""
          
          # Overall status
          if [[ "${{ needs.codeql-analysis.result }}" == "success" && 
                "${{ needs.security-test-suite.result }}" == "success" && 
                "${{ needs.dependency-scan.result }}" == "success" && 
                "${{ needs.secret-scan.result }}" == "success" && 
                "${{ needs.config-security.result }}" == "success" ]]; then
            echo "✅ Overall Security Status: PASSED"
            echo "🛡️  All security checks completed successfully"
          else
            echo "❌ Overall Security Status: FAILED"
            echo "⚠️  One or more security checks failed"
            echo "Please review the individual job logs for details"
          fi

  # Security success notification
  security-success:
    name: Security Success
    runs-on: ubuntu-latest
    needs: [codeql-analysis, security-test-suite, dependency-scan, secret-scan, config-security]
    if: success()
    
    steps:
      - name: Security success notification
        run: |
          echo "🎉 All security checks passed successfully!"
          echo ""
          echo "✅ Static Analysis (CodeQL): PASSED"
          echo "✅ Security Test Suite (25+ tests): PASSED"
          echo "✅ Dependency Vulnerability Scan: PASSED"
          echo "✅ Secret Detection: PASSED"
          echo "✅ Configuration Security: PASSED"
          echo ""
          echo "🛡️  Security Score: EXCELLENT"
          echo "🚀 Code is ready for production deployment!"